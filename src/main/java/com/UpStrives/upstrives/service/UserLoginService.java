package com.UpStrives.upstrives.service;
/*
 * package com.DevLoomX.devloomX.service;
 * 
 * public class UserLoginService {
 * 
 * @Autowired
 * public UserRegistryRepository userRegistryRepository;
 * 
 * // Example method for user login
 * public String LoginUser(UserLoginDto userLoginDto) {
 * // Logic to authenticate user
 * String email = userLoginDto.getEmail();
 * String password = userLoginDto.getPassword();
 * 
 * UserRegistryEntity user = userRegistryRepository.findByEmail(email);
 * 
 * if (user != null && user.getPassword().equals(password)) {
 * return "Login successful for user: " + user.getName();
 * } else {
 * return "Invalid email or password";
 * }
 * }
 * }
 */